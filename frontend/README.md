# FlexGCC Frontend

Simple React + Vite frontend that talks to the backend under /api.

To run:

1. cd frontend
2. npm install
3. npm run dev

The development server proxies /api to http://localhost:5000 — change vite.config.js if your backend runs on a different port.